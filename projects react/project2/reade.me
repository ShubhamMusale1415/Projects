for running the project: npm run dev 
props: these are values we pass to to the components... for changing component wise data 
ex.see example of how usercard values are changed when we passed data from app.jsx file respectively.